1 - aprender a injectar beans nos contraints validators

2 - alterar o update do AbstractCrudService para atualizar o campo active por id, e caso não retorne nenhuma linha
    atualizada, dá o erro por não existir entidade, assim só se vai na base de dados uma vez

3 - Refatorar a lógica do sequence service: não precisa ter o método de update sequence, basta ter o swap sequence

4 - Implementar testes unitários

5 - Implementar testes de integração, com uma base de dados real ou em memória
